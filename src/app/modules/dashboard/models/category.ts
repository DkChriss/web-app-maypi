export interface CategoryStore {
    title: string,
    slug: string
}

export interface CategoryUpdate {
    id: number,
    title: string,
    slug: string
}

export interface Category {
    id: number,
    title: string,
    slug: string
}
